#include "virtual_ocs_device.h"

// The OCS virtual device classes (port / cross-connect / factory-data) are
// small enough to be fully defined inline in virtual_ocs_device.h. This
// translation unit is kept for symmetry and for any future out-of-line methods.
